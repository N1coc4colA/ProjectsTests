#include "visualcontainer.h"

VisualContainer::VisualContainer(QWidget *parent) : QWidget(parent)
{
    /*this->setBlurEnabled(true);
    this->setBlendMode(BlendMode::BehindWindowBlend);
    this->setMode(BlurMode::GaussianBlur);*/
}
